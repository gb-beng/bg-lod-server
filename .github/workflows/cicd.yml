name: CICD

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build-push-image:
    uses: beeldengeluid/gha-workflows/.github/workflows/build-push-image.yml@6d129f89607f6d49fb3ab07e21eef5d875184761
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write
  integration-tests:
    needs: [build-push-image]
    uses: gb-beng/gha-workflows/.github/workflows/trigger-integration-tests.yml@main
    with:
      image: ${{ needs.build-push-image.outputs.oci }}
      workload: beng-lod-server # could be repo/...
  deploy:
    needs: [integration-tests]
    uses: gb-beng/gha-workflows/.github/workflows/trigger-deploy.yml@main
